---
title: "xuan-query 数据库结构"
---

本文记录 `xuan-query` 的数据库结构、表字段、普通索引、初始化数据和 Flyway migration 对应关系。

## 数据库归属

| 项 | 内容 |
| --- | --- |
| 数据库 | `xuan_query` |
| migration 目录 | `xuan-query/src/main/resources/db/migration/` |
| 当前 migration 最新版本 | `V1` |
| 本次新增 migration | `V1__init_query_database.sql` |
| 数据所有权 | 只允许 `xuan-query` 直接写入只读宽表、报表表、聚合读模型和事件消费游标 |

## migration 规则

涉及数据库结构、字段、索引、约束、初始化数据或 Flyway 脚本时，必须先扫描本服务 `db/migration` 目录，确认当前最高版本号，再追加新 migration。禁止跳号、复用版本号、修改历史 migration。

本次扫描结果：本服务历史最高版本为“无”，新增 `V1`，不存在版本冲突。

## 设计说明

- 本服务使用独立数据库 `xuan_query`，服务启动时 Flyway 连接该数据库执行 migration。
- migration 只管理当前连接数据库内的表、索引、约束和初始化数据；不包含 `CREATE DATABASE`，也不创建 PostgreSQL schema。
- 跨服务只保存外部 ID、编码和必要快照字段，不建立跨服务数据库外键。
- 数据库只保留表结构、主键和普通查询索引；外键、唯一性、状态枚举、金额非负等业务约束全部由应用逻辑校验。
- 本服务只保存读模型和搜索/报表所需宽表，数据来源于业务服务事件或同步任务。
- 不得直接跨库 JOIN 业务服务主库。

## 核心表清单

| 表名 | 说明 | 是否包含 tenant_id |
| --- | --- | --- |
| `query_event_cursor` | 读模型事件消费游标。 | 否 |
| `product_stock_read_model` | 商品库存聚合读模型。 | 是 |
| `sale_order_list_read_model` | 销售单列表读模型。 | 是 |
| `purchase_order_list_read_model` | 采购单列表读模型。 | 是 |
| `counterparty_finance_summary` | 往来主体财务汇总读模型。 | 是 |
| `inventory_summary_read_model` | 库存汇总报表读模型。 | 是 |
| `undo_log` | Seata AT 模式回滚日志表 | 否 |

## 初始化数据

V1 不写入业务初始化数据；本服务的业务数据由对应业务流程、事件同步或外部配置产生。

## `query_event_cursor`

读模型事件消费游标。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `consumer_name` | `varchar(120)` | 是 | `` | 消费者名称 |
| `topic` | `varchar(120)` | 是 | `` | 主题 |
| `partition_key` | `varchar(120)` | 是 | `'default'` | 分区键 |
| `last_event_id` | `varchar(128)` | 否 | `` | 最后事件 ID |
| `last_event_at` | `timestamptz` | 否 | `` | 最后事件时间 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_query_event_cursor` | `id` | primary | 主键 |
| `idx_query_event_cursor` | `consumer_name, topic, partition_key` | normal | 消费游标唯一；唯一性由应用逻辑保证 |

## `product_stock_read_model`

商品库存聚合读模型。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `product_id` | `bigint` | 是 | `` | 商品 ID |
| `product_code` | `varchar(100)` | 是 | `` | 商品编码 |
| `product_name` | `varchar(200)` | 是 | `` | 商品名称 |
| `category_id` | `bigint` | 否 | `` | 分类 ID |
| `category_name` | `varchar(200)` | 否 | `` | 分类名称 |
| `warehouse_id` | `bigint` | 否 | `` | 仓库 ID |
| `warehouse_name` | `varchar(200)` | 否 | `` | 仓库名称 |
| `location_id` | `bigint` | 否 | `` | 库位 ID |
| `location_name` | `varchar(200)` | 否 | `` | 库位名称 |
| `qty_on_hand` | `numeric(18,4)` | 是 | `0` | 当前库存 |
| `qty_reserved` | `numeric(18,4)` | 是 | `0` | 锁定库存 |
| `available_qty` | `numeric(18,4)` | 是 | `0` | 可用库存 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_product_stock_read_model` | `id` | primary | 主键 |
| `idx_query_product_stock_key` | `tenant_id, product_id, warehouse_id, COALESCE(location_id, -1)` | normal | 商品仓库库位读模型唯一；唯一性由应用逻辑保证 |
| `idx_query_product_stock_search` | `tenant_id, product_code, product_name` | normal | 商品库存搜索；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `qty_reserved >= 0`。

## `sale_order_list_read_model`

销售单列表读模型。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `order_id` | `bigint` | 是 | `` | 单据 ID |
| `order_no` | `varchar(64)` | 是 | `` | 单据号 |
| `status` | `varchar(20)` | 是 | `` | 状态 |
| `order_at` | `timestamptz` | 否 | `` | 单据时间 |
| `customer_id` | `bigint` | 否 | `` | 往来方 ID |
| `customer_name` | `varchar(200)` | 否 | `` | 往来方名称 |
| `total_amount` | `numeric(18,2)` | 是 | `0` | 总金额 |
| `paid_amount` | `numeric(18,2)` | 是 | `0` | 已结算金额 |
| `discount_amount` | `numeric(18,2)` | 是 | `0` | 优惠金额 |
| `item_count` | `integer` | 是 | `0` | 明细行数 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_sale_order_list_read_model` | `id` | primary | 主键 |
| `idx_query_sale_order_list_read_model_order` | `tenant_id, order_id` | normal | 单据读模型唯一；唯一性由应用逻辑保证 |
| `idx_query_sale_order_list_read_model_page` | `tenant_id, status, customer_id, order_at DESC, order_id DESC` | normal | 列表分页查询；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `total_amount >= 0`。
- 应用层校验 `paid_amount >= 0`。
- 应用层校验 `discount_amount >= 0`。
- 应用层校验 `item_count >= 0`。

## `purchase_order_list_read_model`

采购单列表读模型。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `order_id` | `bigint` | 是 | `` | 单据 ID |
| `order_no` | `varchar(64)` | 是 | `` | 单据号 |
| `status` | `varchar(20)` | 是 | `` | 状态 |
| `order_at` | `timestamptz` | 否 | `` | 单据时间 |
| `supplier_id` | `bigint` | 否 | `` | 往来方 ID |
| `supplier_name` | `varchar(200)` | 否 | `` | 往来方名称 |
| `total_amount` | `numeric(18,2)` | 是 | `0` | 总金额 |
| `paid_amount` | `numeric(18,2)` | 是 | `0` | 已结算金额 |
| `discount_amount` | `numeric(18,2)` | 是 | `0` | 优惠金额 |
| `item_count` | `integer` | 是 | `0` | 明细行数 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_purchase_order_list_read_model` | `id` | primary | 主键 |
| `idx_query_purchase_order_list_read_model_order` | `tenant_id, order_id` | normal | 单据读模型唯一；唯一性由应用逻辑保证 |
| `idx_query_purchase_order_list_read_model_page` | `tenant_id, status, supplier_id, order_at DESC, order_id DESC` | normal | 列表分页查询；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `total_amount >= 0`。
- 应用层校验 `paid_amount >= 0`。
- 应用层校验 `discount_amount >= 0`。
- 应用层校验 `item_count >= 0`。

## `counterparty_finance_summary`

往来主体财务汇总读模型。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `subject_id` | `bigint` | 是 | `` | 往来主体 ID |
| `subject_name` | `varchar(200)` | 是 | `` | 往来主体名称 |
| `receivable_total` | `numeric(18,2)` | 是 | `0` | 应收总额 |
| `payable_total` | `numeric(18,2)` | 是 | `0` | 应付总额 |
| `net_amount` | `numeric(18,2)` | 是 | `0` | 净额 |
| `customer_count` | `bigint` | 是 | `0` | 客户数量 |
| `supplier_count` | `bigint` | 是 | `0` | 供应商数量 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_counterparty_finance_summary` | `id` | primary | 主键 |
| `idx_query_counterparty_finance` | `tenant_id, subject_id` | normal | 往来主体财务汇总唯一；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `receivable_total >= 0`。
- 应用层校验 `payable_total >= 0`。
- 应用层校验 `customer_count >= 0`。
- 应用层校验 `supplier_count >= 0`。

## `inventory_summary_read_model`

库存汇总报表读模型。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `warehouse_id` | `bigint` | 否 | `` | 仓库 ID |
| `warehouse_name` | `varchar(200)` | 否 | `` | 仓库名称 |
| `category_id` | `bigint` | 否 | `` | 分类 ID |
| `category_name` | `varchar(200)` | 否 | `` | 分类名称 |
| `product_count` | `bigint` | 是 | `0` | 商品数 |
| `qty_on_hand` | `numeric(18,4)` | 是 | `0` | 当前库存 |
| `qty_reserved` | `numeric(18,4)` | 是 | `0` | 锁定库存 |
| `available_qty` | `numeric(18,4)` | 是 | `0` | 可用库存 |
| `stock_amount` | `numeric(18,2)` | 是 | `0` | 库存金额 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_inventory_summary_read_model` | `id` | primary | 主键 |
| `idx_query_inventory_summary` | `tenant_id, COALESCE(warehouse_id, -1), COALESCE(category_id, -1)` | normal | 库存汇总维度唯一；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `product_count >= 0`。
- 应用层校验 `qty_reserved >= 0`。
- 应用层校验 `stock_amount >= 0`。

## `undo_log`

Seata AT 模式回滚日志表，用于全局事务回滚。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `branch_id` | `bigint` | 是 | `` | Seata 分支事务 ID |
| `xid` | `varchar(128)` | 是 | `` | Seata 全局事务 ID |
| `context` | `varchar(128)` | 是 | `` | 上下文 |
| `rollback_info` | `bytea` | 是 | `` | 回滚信息 |
| `log_status` | `integer` | 是 | `` | 日志状态 |
| `log_created` | `timestamp(6)` | 是 | `` | 创建时间 |
| `log_modified` | `timestamp(6)` | 是 | `` | 更新时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_undo_log` | `branch_id, xid` | primary | 主键 |
| `idx_undo_log_log_created` | `log_created` | normal | 按创建时间清理和排查回滚日志 |
