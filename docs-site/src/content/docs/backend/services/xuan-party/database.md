---
title: "xuan-party 数据库结构"
---

本文记录 `xuan-party` 的数据库结构、表字段、普通索引、初始化数据和 Flyway migration 对应关系。

## 数据库归属

| 项 | 内容 |
| --- | --- |
| 数据库 | `xuan_party` |
| migration 目录 | `xuan-party/src/main/resources/db/migration/` |
| 当前 migration 最新版本 | `V1` |
| 本次新增 migration | `V1__init_party_database.sql` |
| 数据所有权 | 只允许 `xuan-party` 直接写入客户、供应商、往来主体和导入数据 |

## migration 规则

涉及数据库结构、字段、索引、约束、初始化数据或 Flyway 脚本时，必须先扫描本服务 `db/migration` 目录，确认当前最高版本号，再追加新 migration。禁止跳号、复用版本号、修改历史 migration。

本次扫描结果：本服务历史最高版本为“无”，新增 `V1`，不存在版本冲突。

## 设计说明

- 本服务使用独立数据库 `xuan_party`，服务启动时 Flyway 连接该数据库执行 migration。
- migration 只管理当前连接数据库内的表、索引、约束和初始化数据；不包含 `CREATE DATABASE`，也不创建 PostgreSQL schema。
- 跨服务只保存外部 ID、编码和必要快照字段，不建立跨服务数据库外键。
- 数据库只保留表结构、主键和普通查询索引；外键、唯一性、状态枚举、金额非负等业务约束全部由应用逻辑校验。
- 业务表默认包含 `tenant_id`，平台级或网关级表会在表说明中单独说明。
- 逻辑删除统一使用 `deleted_at`，活动态唯一性由应用层在 `deleted_at IS NULL` 范围内校验。

## 核心表清单

| 表名 | 说明 | 是否包含 tenant_id |
| --- | --- | --- |
| `customer_category` | 客户类别表。 | 是 |
| `counterparty_subject` | 往来主体表，用于统一客户、供应商和财务往来。 | 是 |
| `counterparty_subject_link` | 往来主体关联表。 | 是 |
| `customer` | 客户表。 | 是 |
| `delivery_method` | 送货方式表。 | 是 |
| `supplier_type` | 供应商类型表。 | 是 |
| `supplier` | 供应商表。 | 是 |
| `customer_import_batch` | 客户导入批次。 | 是 |
| `customer_import_item` | 客户导入明细。 | 是 |
| `supplier_import_batch` | 供应商导入批次。 | 是 |
| `supplier_import_item` | 供应商导入明细。 | 是 |
| `undo_log` | Seata AT 模式回滚日志表 | 否 |

## 初始化数据

| 表名 | 初始化条目 | 说明 |
| --- | --- | --- |
| `customer_category` | `GENERAL` | 默认客户类别。 |
| `supplier_type` | `GENERAL` | 默认供应商类型。 |
| `delivery_method` | `PICKUP`, `DOOR_DELIVERY`, `LOGISTICS` | 常用送货方式，归属 xuan-party 维护。 |

## `customer_category`

客户类别表。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `code` | `varchar(100)` | 是 | `` | 类别编码 |
| `name` | `varchar(200)` | 是 | `` | 类别名称 |
| `description` | `varchar(500)` | 否 | `` | 描述 |
| `sort_no` | `integer` | 是 | `0` | 排序 |
| `is_enabled` | `boolean` | 是 | `true` | 是否启用 |
| `is_default` | `boolean` | 是 | `false` | 是否默认 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_customer_category` | `id` | primary | 主键 |
| `idx_party_customer_category_code_active` | `tenant_id, code; WHERE deleted_at IS NULL` | normal | 客户类别编码活动态唯一；唯一性由应用逻辑保证 |
| `idx_party_customer_category_default` | `tenant_id; WHERE is_default = true AND deleted_at IS NULL` | normal | 每租户一个默认客户类别；唯一性由应用逻辑保证 |
| `idx_party_customer_category_tenant_deleted` | `tenant_id, deleted_at` | normal | 租户活动数据过滤；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `sort_no >= 0`。

## `counterparty_subject`

往来主体表，用于统一客户、供应商和财务往来。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `name` | `varchar(200)` | 是 | `` | 主体名称 |
| `region` | `varchar(128)` | 否 | `` | 区域 |
| `unified_credit_code` | `varchar(64)` | 否 | `` | 统一社会信用代码 |
| `is_enabled` | `boolean` | 是 | `true` | 是否启用 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_counterparty_subject` | `id` | primary | 主键 |
| `idx_party_subject_name` | `tenant_id, name` | normal | 主体名称查询；唯一性由应用逻辑保证 |
| `idx_party_subject_tenant_deleted` | `tenant_id, deleted_at` | normal | 租户活动数据过滤；唯一性由应用逻辑保证 |

## `counterparty_subject_link`

往来主体关联表。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `subject_id` | `bigint` | 是 | `` | 往来主体 ID |
| `target_type` | `varchar(32)` | 是 | `` | 关联对象类型 |
| `target_id` | `bigint` | 是 | `` | 关联对象 ID |
| `role_type` | `varchar(32)` | 是 | `` | 业务角色 |
| `is_primary` | `boolean` | 是 | `false` | 是否主档案 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_counterparty_subject_link` | `id` | primary | 主键 |
| `idx_party_subject_link_target` | `tenant_id, target_type, target_id, role_type` | normal | 目标角色关联唯一；唯一性由应用逻辑保证 |
| `idx_party_subject_link_subject` | `tenant_id, subject_id` | normal | 主体关联查询；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `subject_id` 必须引用有效的 `counterparty_subject.id`。

## `customer`

客户表。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `code` | `varchar(100)` | 是 | `` | 客户编码 |
| `name` | `varchar(200)` | 是 | `` | 客户名称 |
| `short_name` | `varchar(100)` | 否 | `` | 客户简称 |
| `category_id` | `bigint` | 否 | `` | 客户类别 ID |
| `counterparty_subject_id` | `bigint` | 否 | `` | 往来主体 ID |
| `contact` | `varchar(100)` | 否 | `` | 联系人 |
| `phone` | `varchar(50)` | 否 | `` | 联系电话 |
| `mobile` | `varchar(50)` | 否 | `` | 手机 |
| `email` | `varchar(200)` | 否 | `` | 邮箱 |
| `address` | `varchar(500)` | 否 | `` | 地址 |
| `tax_no` | `varchar(100)` | 否 | `` | 税号 |
| `bank_name` | `varchar(200)` | 否 | `` | 开户行 |
| `bank_account` | `varchar(200)` | 否 | `` | 银行账号 |
| `invoice_title` | `varchar(200)` | 否 | `` | 发票抬头 |
| `credit_limit` | `numeric(18,2)` | 是 | `0` | 授信额度 |
| `contacts` | `jsonb` | 否 | `` | 联系人 JSON |
| `delivery_method_code` | `varchar(100)` | 否 | `` | 默认送货方式编码 |
| `default_settlement_method_code` | `varchar(100)` | 否 | `` | 默认结算方式编码 |
| `default_receipt_method_code` | `varchar(100)` | 否 | `` | 默认收款方式编码 |
| `is_enabled` | `boolean` | 是 | `true` | 是否启用 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_customer` | `id` | primary | 主键 |
| `idx_party_customer_code_active` | `tenant_id, code; WHERE deleted_at IS NULL` | normal | 客户编码活动态唯一；唯一性由应用逻辑保证 |
| `idx_party_customer_name` | `tenant_id, name` | normal | 客户名称查询；唯一性由应用逻辑保证 |
| `idx_party_customer_category` | `tenant_id, category_id` | normal | 客户类别查询；唯一性由应用逻辑保证 |
| `idx_party_customer_tenant_deleted` | `tenant_id, deleted_at` | normal | 租户活动数据过滤；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `category_id` 必须引用有效的 `customer_category.id`。
- 应用层校验 `counterparty_subject_id` 必须引用有效的 `counterparty_subject.id`。
- 应用层校验 `credit_limit >= 0`。

### 跨服务引用

- 送货方式只保存 code，由 `xuan-party.delivery_method` 解释；结算方式、收款方式只保存 code，由 `xuan-finance` 解释。

## `delivery_method`

送货方式表。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `code` | `varchar(100)` | 是 | `` | 送货方式编码 |
| `name` | `varchar(200)` | 是 | `` | 送货方式名称 |
| `sort_no` | `integer` | 是 | `0` | 排序 |
| `is_enabled` | `boolean` | 是 | `true` | 是否启用 |
| `is_default` | `boolean` | 是 | `false` | 是否默认 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_delivery_method` | `id` | primary | 主键 |
| `idx_delivery_met_code_active` | `tenant_id, code; WHERE deleted_at IS NULL` | normal | 送货方式编码活动态唯一；唯一性由应用逻辑保证 |
| `idx_delivery_method_default` | `tenant_id; WHERE is_default = true AND deleted_at IS NULL` | normal | 每租户一个默认送货方式；唯一性由应用逻辑保证 |
| `idx_delivery_met_delivery_method_tenant_deleted` | `tenant_id, deleted_at` | normal | 租户活动数据过滤；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `sort_no >= 0`。

## `supplier_type`

供应商类型表。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `code` | `varchar(100)` | 是 | `` | 类型编码 |
| `name` | `varchar(200)` | 是 | `` | 类型名称 |
| `is_enabled` | `boolean` | 是 | `true` | 是否启用 |
| `sort_no` | `integer` | 是 | `0` | 排序 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_supplier_type` | `id` | primary | 主键 |
| `idx_party_supplier_type_code_active` | `tenant_id, code; WHERE deleted_at IS NULL` | normal | 供应商类型编码活动态唯一；唯一性由应用逻辑保证 |
| `idx_party_supplier_type_tenant_deleted` | `tenant_id, deleted_at` | normal | 租户活动数据过滤；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `sort_no >= 0`。

## `supplier`

供应商表。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `code` | `varchar(100)` | 是 | `` | 供应商编码 |
| `name` | `varchar(200)` | 是 | `` | 供应商名称 |
| `short_name` | `varchar(100)` | 否 | `` | 简称 |
| `supplier_type_id` | `bigint` | 否 | `` | 供应商类型 ID |
| `counterparty_subject_id` | `bigint` | 否 | `` | 往来主体 ID |
| `contact` | `varchar(100)` | 否 | `` | 联系人 |
| `phone` | `varchar(50)` | 否 | `` | 联系电话 |
| `mobile` | `varchar(50)` | 否 | `` | 手机 |
| `email` | `varchar(200)` | 否 | `` | 邮箱 |
| `address` | `varchar(500)` | 否 | `` | 地址 |
| `tax_no` | `varchar(100)` | 否 | `` | 税号 |
| `bank_name` | `varchar(200)` | 否 | `` | 开户行 |
| `bank_account` | `varchar(200)` | 否 | `` | 银行账号 |
| `contacts` | `jsonb` | 否 | `` | 联系人 JSON |
| `is_enabled` | `boolean` | 是 | `true` | 是否启用 |
| `is_blacklisted` | `boolean` | 是 | `false` | 是否黑名单 |
| `default_settlement_method_code` | `varchar(100)` | 否 | `` | 默认结算方式编码 |
| `default_payment_method_code` | `varchar(100)` | 否 | `` | 默认付款方式编码 |
| `region` | `varchar(128)` | 否 | `` | 区域 |
| `wechat` | `varchar(128)` | 否 | `` | 微信客服 |
| `purchaser` | `varchar(128)` | 否 | `` | 采购员 |
| `business_scope` | `varchar(32)` | 是 | `'SUPPLIER'` | 往来类别 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_supplier` | `id` | primary | 主键 |
| `idx_party_supplier_code_active` | `tenant_id, code; WHERE deleted_at IS NULL` | normal | 供应商编码活动态唯一；唯一性由应用逻辑保证 |
| `idx_party_supplier_name` | `tenant_id, name` | normal | 供应商名称查询；唯一性由应用逻辑保证 |
| `idx_party_supplier_type` | `tenant_id, supplier_type_id` | normal | 供应商类型查询；唯一性由应用逻辑保证 |
| `idx_party_supplier_status` | `tenant_id, is_enabled, is_blacklisted, deleted_at` | normal | 供应商状态查询；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `supplier_type_id` 必须引用有效的 `supplier_type.id`。
- 应用层校验 `counterparty_subject_id` 必须引用有效的 `counterparty_subject.id`。

### 跨服务引用

- 结算方式、付款方式只保存 code，由 `xuan-finance` 解释。

## `customer_import_batch`

客户导入批次。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `batch_no` | `varchar(64)` | 是 | `` | 导入批次号 |
| `source_name` | `varchar(255)` | 否 | `` | 来源文件名 |
| `import_mode` | `varchar(32)` | 是 | `'EXCEL_UPLOAD'` | 导入模式 |
| `total_count` | `integer` | 是 | `0` | 总数 |
| `success_count` | `integer` | 是 | `0` | 成功数 |
| `failed_count` | `integer` | 是 | `0` | 失败数 |
| `warning_count` | `integer` | 是 | `0` | 警告数 |
| `status` | `varchar(32)` | 是 | `'PENDING'` | 状态 |
| `summary` | `text` | 否 | `` | 导入摘要 |
| `raw_payload` | `jsonb` | 否 | `` | 原始载荷 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_customer_import_batch` | `id` | primary | 主键 |
| `idx_customer_imp_batch_no_active` | `tenant_id, batch_no; WHERE deleted_at IS NULL` | normal | 批次号活动态唯一；唯一性由应用逻辑保证 |
| `idx_customer_import_batch_status` | `tenant_id, status; WHERE deleted_at IS NULL` | normal | 导入状态查询；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `status IN ('PENDING','PROCESSING','SUCCESS','FAILED','PARTIAL_SUCCESS')`。
- 应用层校验 `total_count >= 0`。
- 应用层校验 `success_count >= 0`。
- 应用层校验 `failed_count >= 0`。
- 应用层校验 `warning_count >= 0`。
- 应用层校验 `success_count + failed_count <= total_count`。

## `customer_import_item`

客户导入明细。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `batch_id` | `bigint` | 是 | `` | 导入批次 ID |
| `row_no` | `integer` | 是 | `` | 行号 |
| `source_code` | `varchar(128)` | 否 | `` | 来源编码 |
| `source_name` | `varchar(255)` | 否 | `` | 来源名称 |
| `matched_target_id` | `bigint` | 否 | `` | 匹配目标 ID |
| `status` | `varchar(32)` | 是 | `'PENDING'` | 状态 |
| `error_field` | `varchar(128)` | 否 | `` | 错误字段 |
| `error_message` | `text` | 否 | `` | 错误信息 |
| `warning_message` | `text` | 否 | `` | 警告信息 |
| `raw_row` | `jsonb` | 是 | `` | 原始行 |
| `normalized_payload` | `jsonb` | 否 | `` | 规范化数据 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_customer_import_item` | `id` | primary | 主键 |
| `idx_customer_imp_batch_row_active` | `tenant_id, batch_id, row_no; WHERE deleted_at IS NULL` | normal | 同批次行号活动态唯一；唯一性由应用逻辑保证 |
| `idx_customer_import_item_status` | `tenant_id, status; WHERE deleted_at IS NULL` | normal | 导入明细状态查询；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `batch_id` 必须引用有效的 `customer_import_batch.id`。
- 应用层校验 `status IN ('PENDING','PROCESSING','SUCCESS','FAILED','PARTIAL_SUCCESS')`。
- 应用层校验 `row_no >= 1`。

## `supplier_import_batch`

供应商导入批次。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `batch_no` | `varchar(64)` | 是 | `` | 导入批次号 |
| `source_name` | `varchar(255)` | 否 | `` | 来源文件名 |
| `import_mode` | `varchar(32)` | 是 | `'EXCEL_UPLOAD'` | 导入模式 |
| `total_count` | `integer` | 是 | `0` | 总数 |
| `success_count` | `integer` | 是 | `0` | 成功数 |
| `failed_count` | `integer` | 是 | `0` | 失败数 |
| `warning_count` | `integer` | 是 | `0` | 警告数 |
| `status` | `varchar(32)` | 是 | `'PENDING'` | 状态 |
| `summary` | `text` | 否 | `` | 导入摘要 |
| `raw_payload` | `jsonb` | 否 | `` | 原始载荷 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_supplier_import_batch` | `id` | primary | 主键 |
| `idx_supplier_imp_batch_no_active` | `tenant_id, batch_no; WHERE deleted_at IS NULL` | normal | 批次号活动态唯一；唯一性由应用逻辑保证 |
| `idx_supplier_import_batch_status` | `tenant_id, status; WHERE deleted_at IS NULL` | normal | 导入状态查询；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `status IN ('PENDING','PROCESSING','SUCCESS','FAILED','PARTIAL_SUCCESS')`。
- 应用层校验 `total_count >= 0`。
- 应用层校验 `success_count >= 0`。
- 应用层校验 `failed_count >= 0`。
- 应用层校验 `warning_count >= 0`。
- 应用层校验 `success_count + failed_count <= total_count`。

## `supplier_import_item`

供应商导入明细。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `batch_id` | `bigint` | 是 | `` | 导入批次 ID |
| `row_no` | `integer` | 是 | `` | 行号 |
| `source_code` | `varchar(128)` | 否 | `` | 来源编码 |
| `source_name` | `varchar(255)` | 否 | `` | 来源名称 |
| `matched_target_id` | `bigint` | 否 | `` | 匹配目标 ID |
| `status` | `varchar(32)` | 是 | `'PENDING'` | 状态 |
| `error_field` | `varchar(128)` | 否 | `` | 错误字段 |
| `error_message` | `text` | 否 | `` | 错误信息 |
| `warning_message` | `text` | 否 | `` | 警告信息 |
| `raw_row` | `jsonb` | 是 | `` | 原始行 |
| `normalized_payload` | `jsonb` | 否 | `` | 规范化数据 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_supplier_import_item` | `id` | primary | 主键 |
| `idx_supplier_imp_batch_row_active` | `tenant_id, batch_id, row_no; WHERE deleted_at IS NULL` | normal | 同批次行号活动态唯一；唯一性由应用逻辑保证 |
| `idx_supplier_import_item_status` | `tenant_id, status; WHERE deleted_at IS NULL` | normal | 导入明细状态查询；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `batch_id` 必须引用有效的 `supplier_import_batch.id`。
- 应用层校验 `status IN ('PENDING','PROCESSING','SUCCESS','FAILED','PARTIAL_SUCCESS')`。
- 应用层校验 `row_no >= 1`。

## `undo_log`

Seata AT 模式回滚日志表，用于全局事务回滚。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `branch_id` | `bigint` | 是 | `` | Seata 分支事务 ID |
| `xid` | `varchar(128)` | 是 | `` | Seata 全局事务 ID |
| `context` | `varchar(128)` | 是 | `` | 上下文 |
| `rollback_info` | `bytea` | 是 | `` | 回滚信息 |
| `log_status` | `integer` | 是 | `` | 日志状态 |
| `log_created` | `timestamp(6)` | 是 | `` | 创建时间 |
| `log_modified` | `timestamp(6)` | 是 | `` | 更新时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_undo_log` | `branch_id, xid` | primary | 主键 |
| `idx_undo_log_log_created` | `log_created` | normal | 按创建时间清理和排查回滚日志 |
