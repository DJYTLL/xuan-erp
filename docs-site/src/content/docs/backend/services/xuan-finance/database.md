---
title: "xuan-finance 数据库结构"
---

本文记录 `xuan-finance` 的数据库结构、表字段、普通索引、初始化数据和 Flyway migration 对应关系。

## 数据库归属

| 项 | 内容 |
| --- | --- |
| 数据库 | `xuan_finance` |
| migration 目录 | `xuan-finance/src/main/resources/db/migration/` |
| 当前 migration 最新版本 | `V1` |
| 本次新增 migration | `V1__init_finance_database.sql` |
| 数据所有权 | 只允许 `xuan-finance` 直接写入应收、应付、收款、付款和财务配置 |

## migration 规则

涉及数据库结构、字段、索引、约束、初始化数据或 Flyway 脚本时，必须先扫描本服务 `db/migration` 目录，确认当前最高版本号，再追加新 migration。禁止跳号、复用版本号、修改历史 migration。

本次扫描结果：本服务历史最高版本为“无”，新增 `V1`，不存在版本冲突。

## 设计说明

- 本服务使用独立数据库 `xuan_finance`，服务启动时 Flyway 连接该数据库执行 migration。
- migration 只管理当前连接数据库内的表、索引、约束和初始化数据；不包含 `CREATE DATABASE`，也不创建 PostgreSQL schema。
- 跨服务只保存外部 ID、编码和必要快照字段，不建立跨服务数据库外键。
- 数据库只保留表结构、主键和普通查询索引；外键、唯一性、状态枚举、金额非负等业务约束全部由应用逻辑校验。
- 财务单据保存销售/采购/往来主体外部 ID 和快照，不跨服务建外键。

## 核心表清单

| 表名 | 说明 | 是否包含 tenant_id |
| --- | --- | --- |
| `settlement_method` | 结算方式表。 | 是 |
| `receipt_method` | 收款方式表。 | 是 |
| `payment_method` | 付款方式表。 | 是 |
| `accounts_receivable` | ERP 应收单。 | 是 |
| `accounts_payable` | ERP 应付单。 | 是 |
| `receipt` | 收款单。 | 是 |
| `receipt_receivable` | 收款单-应收分摊。 | 是 |
| `payment` | 付款单。 | 是 |
| `payment_payable` | 付款单-应付分摊。 | 是 |
| `undo_log` | Seata AT 模式回滚日志表 | 否 |

## 初始化数据

| 表名 | 初始化条目 | 说明 |
| --- | --- | --- |
| `settlement_method` | `CASH`, `MONTHLY`, `CREDIT` | 常用结算方式；`fund_input_mode` 不加枚举约束，由业务解释。 |
| `receipt_method` | `CASH`, `WECHAT`, `ALIPAY`, `BANK_TRANSFER` | 常用收款方式。 |
| `payment_method` | `CASH`, `WECHAT`, `ALIPAY`, `BANK_TRANSFER` | 常用付款方式。 |

## `settlement_method`

结算方式表。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `code` | `varchar(100)` | 是 | `` | 结算方式编码 |
| `name` | `varchar(200)` | 是 | `` | 结算方式名称 |
| `sort_no` | `integer` | 是 | `0` | 排序 |
| `is_enabled` | `boolean` | 是 | `true` | 是否启用 |
| `is_default` | `boolean` | 是 | `false` | 是否默认 |
| `fund_input_mode` | `varchar(20)` | 是 | `'OPTIONAL'` | 即时收付款录入模式 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_settlement_method` | `id` | primary | 主键 |
| `idx_settlement_m_code_active` | `tenant_id, code; WHERE deleted_at IS NULL` | normal | 结算方式编码活动态唯一；唯一性由应用逻辑保证 |
| `idx_settlement_method_default` | `tenant_id; WHERE is_default = true AND deleted_at IS NULL` | normal | 每租户一个默认结算方式；唯一性由应用逻辑保证 |
| `idx_settlement_m_settlement_method_tenant_deleted` | `tenant_id, deleted_at` | normal | 租户活动数据过滤；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `sort_no >= 0`。

## `receipt_method`

收款方式表。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `code` | `varchar(100)` | 是 | `` | 收款方式编码 |
| `name` | `varchar(200)` | 是 | `` | 收款方式名称 |
| `sort_no` | `integer` | 是 | `0` | 排序 |
| `is_enabled` | `boolean` | 是 | `true` | 是否启用 |
| `is_default` | `boolean` | 是 | `false` | 是否默认 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_receipt_method` | `id` | primary | 主键 |
| `idx_receipt_meth_code_active` | `tenant_id, code; WHERE deleted_at IS NULL` | normal | 收款方式编码活动态唯一；唯一性由应用逻辑保证 |
| `idx_receipt_method_default` | `tenant_id; WHERE is_default = true AND deleted_at IS NULL` | normal | 每租户一个默认收款方式；唯一性由应用逻辑保证 |
| `idx_receipt_meth_receipt_method_tenant_deleted` | `tenant_id, deleted_at` | normal | 租户活动数据过滤；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `sort_no >= 0`。

## `payment_method`

付款方式表。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `code` | `varchar(100)` | 是 | `` | 付款方式编码 |
| `name` | `varchar(200)` | 是 | `` | 付款方式名称 |
| `sort_no` | `integer` | 是 | `0` | 排序 |
| `is_enabled` | `boolean` | 是 | `true` | 是否启用 |
| `is_default` | `boolean` | 是 | `false` | 是否默认 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_payment_method` | `id` | primary | 主键 |
| `idx_payment_meth_code_active` | `tenant_id, code; WHERE deleted_at IS NULL` | normal | 付款方式编码活动态唯一；唯一性由应用逻辑保证 |
| `idx_payment_method_default` | `tenant_id; WHERE is_default = true AND deleted_at IS NULL` | normal | 每租户一个默认付款方式；唯一性由应用逻辑保证 |
| `idx_payment_meth_payment_method_tenant_deleted` | `tenant_id, deleted_at` | normal | 租户活动数据过滤；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `sort_no >= 0`。

## `accounts_receivable`

ERP 应收单。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `sale_order_id` | `bigint` | 否 | `` | 来源单据 ID |
| `order_no` | `varchar(64)` | 是 | `` | 来源单据号 |
| `customer_id` | `bigint` | 是 | `` | 客户 ID |
| `party_name_snapshot` | `varchar(200)` | 否 | `` | 往来方名称快照 |
| `total_amount` | `numeric(18,2)` | 是 | `0` | 总金额 |
| `paid_amount` | `numeric(18,2)` | 是 | `0` | 已结算金额 |
| `unpaid_amount` | `numeric(18,2)` | 是 | `0` | 未结算金额 |
| `discount_amount` | `numeric(18,2)` | 是 | `0` | 优惠金额 |
| `status` | `varchar(20)` | 是 | `'OPEN'` | 状态 |
| `settlement_method_code` | `varchar(100)` | 否 | `` | 结算方式编码 |
| `source_document_type` | `varchar(50)` | 否 | `` | 自动联动来源单据类型 |
| `source_document_id` | `bigint` | 否 | `` | 自动联动来源单据 ID |
| `source_business_flow` | `varchar(50)` | 否 | `` | 自动联动业务流 |
| `auto_flow_generated` | `boolean` | 是 | `false` | 是否自动生成 |
| `auto_flow_mode` | `varchar(50)` | 否 | `` | 自动生成模式 |
| `auto_flow_managed_state` | `varchar(50)` | 否 | `` | 自动托管状态 |
| `print_count` | `integer` | 是 | `0` | 打印次数 |
| `last_printed_at` | `timestamptz` | 否 | `` | 最后打印时间 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_accounts_receivable` | `id` | primary | 主键 |
| `idx_finance_ar_order_no_active` | `tenant_id, order_no; WHERE deleted_at IS NULL` | normal | 来源单据号活动态唯一；唯一性由应用逻辑保证 |
| `idx_finance_ar_party` | `tenant_id, customer_id, status, created_at DESC; WHERE deleted_at IS NULL` | normal | 往来方账款查询；唯一性由应用逻辑保证 |
| `idx_finance_ar_auto_flow` | `tenant_id, source_document_type, source_document_id, source_business_flow; WHERE auto_flow_generated = true AND deleted_at IS NULL` | normal | 自动联动来源查询；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `status IN ('OPEN','PARTIAL','CLOSED','CANCELLED')`。
- 应用层校验 `total_amount >= 0`。
- 应用层校验 `paid_amount >= 0`。
- 应用层校验 `unpaid_amount >= 0`。
- 应用层校验 `discount_amount >= 0`。
- 应用层校验 `print_count >= 0`。
- 应用层校验 `paid_amount + discount_amount <= total_amount`。
- 应用层校验 `unpaid_amount = total_amount - paid_amount - discount_amount`。

## `accounts_payable`

ERP 应付单。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `purchase_order_id` | `bigint` | 否 | `` | 来源单据 ID |
| `order_no` | `varchar(64)` | 是 | `` | 来源单据号 |
| `supplier_id` | `bigint` | 是 | `` | 供应商 ID |
| `party_name_snapshot` | `varchar(200)` | 否 | `` | 往来方名称快照 |
| `total_amount` | `numeric(18,2)` | 是 | `0` | 总金额 |
| `paid_amount` | `numeric(18,2)` | 是 | `0` | 已结算金额 |
| `unpaid_amount` | `numeric(18,2)` | 是 | `0` | 未结算金额 |
| `discount_amount` | `numeric(18,2)` | 是 | `0` | 优惠金额 |
| `status` | `varchar(20)` | 是 | `'OPEN'` | 状态 |
| `settlement_method_code` | `varchar(100)` | 否 | `` | 结算方式编码 |
| `source_document_type` | `varchar(50)` | 否 | `` | 自动联动来源单据类型 |
| `source_document_id` | `bigint` | 否 | `` | 自动联动来源单据 ID |
| `source_business_flow` | `varchar(50)` | 否 | `` | 自动联动业务流 |
| `auto_flow_generated` | `boolean` | 是 | `false` | 是否自动生成 |
| `auto_flow_mode` | `varchar(50)` | 否 | `` | 自动生成模式 |
| `auto_flow_managed_state` | `varchar(50)` | 否 | `` | 自动托管状态 |
| `print_count` | `integer` | 是 | `0` | 打印次数 |
| `last_printed_at` | `timestamptz` | 否 | `` | 最后打印时间 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_accounts_payable` | `id` | primary | 主键 |
| `idx_finance_ap_order_no_active` | `tenant_id, order_no; WHERE deleted_at IS NULL` | normal | 来源单据号活动态唯一；唯一性由应用逻辑保证 |
| `idx_finance_ap_party` | `tenant_id, supplier_id, status, created_at DESC; WHERE deleted_at IS NULL` | normal | 往来方账款查询；唯一性由应用逻辑保证 |
| `idx_finance_ap_auto_flow` | `tenant_id, source_document_type, source_document_id, source_business_flow; WHERE auto_flow_generated = true AND deleted_at IS NULL` | normal | 自动联动来源查询；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `status IN ('OPEN','PARTIAL','CLOSED','CANCELLED')`。
- 应用层校验 `total_amount >= 0`。
- 应用层校验 `paid_amount >= 0`。
- 应用层校验 `unpaid_amount >= 0`。
- 应用层校验 `discount_amount >= 0`。
- 应用层校验 `print_count >= 0`。
- 应用层校验 `paid_amount + discount_amount <= total_amount`。
- 应用层校验 `unpaid_amount = total_amount - paid_amount - discount_amount`。

## `receipt`

收款单。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `receipt_no` | `varchar(64)` | 是 | `` | 单号 |
| `customer_id` | `bigint` | 是 | `` | 客户 ID |
| `party_name_snapshot` | `varchar(200)` | 否 | `` | 往来方名称快照 |
| `amount` | `numeric(18,2)` | 是 | `0` | 收付款金额 |
| `discount_amount` | `numeric(18,2)` | 是 | `0` | 优惠金额 |
| `settlement_method_code` | `varchar(100)` | 否 | `` | 结算方式编码 |
| `receipt_method_code` | `varchar(100)` | 否 | `` | 收付款方式编码 |
| `status` | `varchar(20)` | 是 | `'APPROVED'` | 状态 |
| `occurred_at` | `timestamptz` | 否 | `` | 收付款时间 |
| `source_document_type` | `varchar(50)` | 否 | `` | 自动联动来源单据类型 |
| `source_document_id` | `bigint` | 否 | `` | 自动联动来源单据 ID |
| `source_business_flow` | `varchar(50)` | 否 | `` | 自动联动业务流 |
| `auto_flow_generated` | `boolean` | 是 | `false` | 是否自动生成 |
| `auto_flow_mode` | `varchar(50)` | 否 | `` | 自动生成模式 |
| `auto_flow_managed_state` | `varchar(50)` | 否 | `` | 自动托管状态 |
| `print_count` | `integer` | 是 | `0` | 打印次数 |
| `last_printed_at` | `timestamptz` | 否 | `` | 最后打印时间 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_receipt` | `id` | primary | 主键 |
| `idx_finance_receipt_no_active` | `tenant_id, receipt_no; WHERE deleted_at IS NULL` | normal | 收付款单号活动态唯一；唯一性由应用逻辑保证 |
| `idx_finance_receipt_party` | `tenant_id, customer_id, created_at DESC; WHERE deleted_at IS NULL` | normal | 往来方收付款查询；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `status IN ('DRAFT','APPROVED','CANCELLED','RED_FLUSHED')`。
- 应用层校验 `amount >= 0`。
- 应用层校验 `discount_amount >= 0`。
- 应用层校验 `print_count >= 0`。

## `receipt_receivable`

收款单-应收分摊。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `receipt_id` | `bigint` | 是 | `` | 收付款单 ID |
| `receivable_id` | `bigint` | 是 | `` | 账款单 ID |
| `allocated_amount` | `numeric(18,2)` | 是 | `0` | 分摊金额 |
| `allocated_discount` | `numeric(18,2)` | 是 | `0` | 分摊优惠 |
| `allocated_total` | `numeric(18,2)` | 是 | `0` | 分摊合计 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_receipt_receivable` | `id` | primary | 主键 |
| `idx_finance_receipt_receivable_active` | `tenant_id, receipt_id, receivable_id; WHERE deleted_at IS NULL` | normal | 分摊关系活动态唯一；唯一性由应用逻辑保证 |
| `idx_finance_receipt_receivable_target` | `tenant_id, receivable_id` | normal | 账款分摊查询；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `receipt_id` 必须引用有效的 `receipt.id`。
- 应用层校验 `receivable_id` 必须引用有效的 `accounts_receivable.id`。
- 应用层校验 `allocated_amount >= 0`。
- 应用层校验 `allocated_discount >= 0`。
- 应用层校验 `allocated_total >= 0`。
- 应用层校验 `allocated_total = allocated_amount + allocated_discount`。

## `payment`

付款单。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `payment_no` | `varchar(64)` | 是 | `` | 单号 |
| `supplier_id` | `bigint` | 是 | `` | 供应商 ID |
| `party_name_snapshot` | `varchar(200)` | 否 | `` | 往来方名称快照 |
| `amount` | `numeric(18,2)` | 是 | `0` | 收付款金额 |
| `discount_amount` | `numeric(18,2)` | 是 | `0` | 优惠金额 |
| `settlement_method_code` | `varchar(100)` | 否 | `` | 结算方式编码 |
| `payment_method_code` | `varchar(100)` | 否 | `` | 收付款方式编码 |
| `status` | `varchar(20)` | 是 | `'APPROVED'` | 状态 |
| `occurred_at` | `timestamptz` | 否 | `` | 收付款时间 |
| `source_document_type` | `varchar(50)` | 否 | `` | 自动联动来源单据类型 |
| `source_document_id` | `bigint` | 否 | `` | 自动联动来源单据 ID |
| `source_business_flow` | `varchar(50)` | 否 | `` | 自动联动业务流 |
| `auto_flow_generated` | `boolean` | 是 | `false` | 是否自动生成 |
| `auto_flow_mode` | `varchar(50)` | 否 | `` | 自动生成模式 |
| `auto_flow_managed_state` | `varchar(50)` | 否 | `` | 自动托管状态 |
| `print_count` | `integer` | 是 | `0` | 打印次数 |
| `last_printed_at` | `timestamptz` | 否 | `` | 最后打印时间 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_payment` | `id` | primary | 主键 |
| `idx_finance_payment_no_active` | `tenant_id, payment_no; WHERE deleted_at IS NULL` | normal | 收付款单号活动态唯一；唯一性由应用逻辑保证 |
| `idx_finance_payment_party` | `tenant_id, supplier_id, created_at DESC; WHERE deleted_at IS NULL` | normal | 往来方收付款查询；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `status IN ('DRAFT','APPROVED','CANCELLED','RED_FLUSHED')`。
- 应用层校验 `amount >= 0`。
- 应用层校验 `discount_amount >= 0`。
- 应用层校验 `print_count >= 0`。

## `payment_payable`

付款单-应付分摊。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `payment_id` | `bigint` | 是 | `` | 收付款单 ID |
| `payable_id` | `bigint` | 是 | `` | 账款单 ID |
| `allocated_amount` | `numeric(18,2)` | 是 | `0` | 分摊金额 |
| `allocated_discount` | `numeric(18,2)` | 是 | `0` | 分摊优惠 |
| `allocated_total` | `numeric(18,2)` | 是 | `0` | 分摊合计 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_payment_payable` | `id` | primary | 主键 |
| `idx_finance_payment_payable_active` | `tenant_id, payment_id, payable_id; WHERE deleted_at IS NULL` | normal | 分摊关系活动态唯一；唯一性由应用逻辑保证 |
| `idx_finance_payment_payable_target` | `tenant_id, payable_id` | normal | 账款分摊查询；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `payment_id` 必须引用有效的 `payment.id`。
- 应用层校验 `payable_id` 必须引用有效的 `accounts_payable.id`。
- 应用层校验 `allocated_amount >= 0`。
- 应用层校验 `allocated_discount >= 0`。
- 应用层校验 `allocated_total >= 0`。
- 应用层校验 `allocated_total = allocated_amount + allocated_discount`。

## `undo_log`

Seata AT 模式回滚日志表，用于全局事务回滚。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `branch_id` | `bigint` | 是 | `` | Seata 分支事务 ID |
| `xid` | `varchar(128)` | 是 | `` | Seata 全局事务 ID |
| `context` | `varchar(128)` | 是 | `` | 上下文 |
| `rollback_info` | `bytea` | 是 | `` | 回滚信息 |
| `log_status` | `integer` | 是 | `` | 日志状态 |
| `log_created` | `timestamp(6)` | 是 | `` | 创建时间 |
| `log_modified` | `timestamp(6)` | 是 | `` | 更新时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_undo_log` | `branch_id, xid` | primary | 主键 |
| `idx_undo_log_log_created` | `log_created` | normal | 按创建时间清理和排查回滚日志 |
