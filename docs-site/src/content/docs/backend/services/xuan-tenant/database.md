---
title: "xuan-tenant 数据库结构"
---

本文记录 `xuan-tenant` 的数据库结构、表字段、普通索引、初始化数据和 Flyway migration 对应关系。

## 数据库归属

| 项 | 内容 |
| --- | --- |
| 数据库 | `xuan_tenant` |
| migration 目录 | `xuan-tenant/src/main/resources/db/migration/` |
| 当前 migration 最新版本 | `V1` |
| 本次新增 migration | `V1__init_tenant_database.sql` |
| 数据所有权 | 只允许 `xuan-tenant` 直接写入租户生命周期和租户级业务配置 |

## migration 规则

涉及数据库结构、字段、索引、约束、初始化数据或 Flyway 脚本时，必须先扫描本服务 `db/migration` 目录，确认当前最高版本号，再追加新 migration。禁止跳号、复用版本号、修改历史 migration。

本次扫描结果：本服务历史最高版本为“无”，新增 `V1`，不存在版本冲突。

## 设计说明

- 本服务使用独立数据库 `xuan_tenant`，服务启动时 Flyway 连接该数据库执行 migration。
- migration 只管理当前连接数据库内的表、索引、约束和初始化数据；不包含 `CREATE DATABASE`，也不创建 PostgreSQL schema。
- 跨服务只保存外部 ID、编码和必要快照字段，不建立跨服务数据库外键。
- 数据库只保留表结构、主键和普通查询索引；外键、唯一性、状态枚举、金额非负等业务约束全部由应用逻辑校验。
- 业务表默认包含 `tenant_id`，平台级或网关级表会在表说明中单独说明。
- 逻辑删除统一使用 `deleted_at`，活动态唯一性由应用层在 `deleted_at IS NULL` 范围内校验。

## 核心表清单

| 表名 | 说明 | 是否包含 tenant_id |
| --- | --- | --- |
| `tenant` | 租户主档，管理租户编码、名称和启停状态。 | 否 |
| `tenant_plan` | 租户套餐定义表，管理套餐编码、价格周期、额度和功能开关。 | 否 |
| `tenant_plan_assignment` | 租户套餐分配表，记录租户当前套餐、生效时间和变更来源。 | 是 |
| `tenant_domain` | 租户域名表，支持租户自定义域名、独立入口和域名验证状态。 | 是 |
| `tenant_contact` | 租户联系人表，支持管理员、商务、技术、财务等多类型联系人。 | 是 |
| `tenant_status_history` | 租户生命周期历史表，记录租户状态变化、原因、操作者和链路信息。 | 是 |
| `tenant_config` | 租户级系统配置，承接旧库 app_system_config 的租户隔离配置能力。 | 是 |
| `tenant_provision_task` | 租户初始化任务表，记录租户开通时跨服务初始化步骤、幂等键、重试和失败原因。 | 是 |
| `tenant_provision_task_step` | 租户初始化任务步骤表，记录初始化任务下每个服务或业务步骤的状态、重试和结果。 | 是 |
| `tenant_outbox_event` | 租户服务 Outbox 事件表，在本地事务内记录待发布领域事件，避免业务写入成功但事件丢失。 | 是 |

## 初始化数据

| 表名 | 初始化条目 | 说明 |
| --- | --- | --- |
| `tenant` | `default` | 默认租户，供本地联调和首个租户初始化使用。 |
| `tenant_status_history` | `default` ENABLED | 默认租户启用生命周期记录。 |
| `tenant_contact` | `default` admin | 默认租户管理员联系人，供本地联调和首个租户初始化使用。 |
| `tenant_plan` | `FREE` | 默认基础套餐，供默认租户和本地联调使用。 |
| `tenant_domain` | `default.local` | 默认租户本地域名占位，后续自定义域名流程可替换或新增。 |
| `tenant_plan_assignment` | `default` -> `FREE` | 默认租户绑定基础套餐。 |
| `tenant_provision_task` | `DEFAULT_BOOTSTRAP` | 默认租户 V1 基线初始化记录。 |
| `tenant_provision_task_step` | `DEFAULT_BOOTSTRAP` / `V1_BASELINE` | 默认租户 V1 基线初始化步骤记录。 |
| `tenant_config` | `system.name`, `locale`, `timezone` | 默认租户的系统名称、语言和时区基础配置。 |

## `tenant`

租户主档，管理租户编码、名称和启停状态。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `code` | `varchar(100)` | 是 | `` | 租户编码 |
| `normalized_code` | `varchar(100)` | 是 | `` | 规范化租户编码，应用层统一小写并校验活动态唯一 |
| `name` | `varchar(200)` | 是 | `` | 租户名称 |
| `status` | `varchar(20)` | 是 | `'PROVISIONING'` | 租户状态：PROVISIONING/ENABLED/SUSPENDED/DISABLED |
| `contact_name` | `varchar(100)` | 否 | `` | 联系人 |
| `contact_phone` | `varchar(50)` | 否 | `` | 联系电话 |
| `provisioned_at` | `timestamptz` | 否 | `` | 开通完成时间 |
| `enabled_at` | `timestamptz` | 否 | `` | 启用时间 |
| `disabled_at` | `timestamptz` | 否 | `` | 停用时间 |
| `disabled_reason` | `varchar(500)` | 否 | `` | 停用原因 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_tenant` | `id` | primary | 主键 |
| `idx_tenant_code_active` | `normalized_code; WHERE deleted_at IS NULL` | normal | 规范化租户编码活动态唯一，由应用逻辑保证；唯一性由应用逻辑保证 |
| `idx_tenant_status` | `status, deleted_at` | normal | 按状态筛选租户；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `status IN ('PROVISIONING', 'ENABLED', 'SUSPENDED', 'DISABLED')`。
- 应用层校验 `normalized_code = lower(code)`。
- 应用层校验 `enabled_at IS NULL OR provisioned_at IS NOT NULL`。
- 应用层校验 `disabled_at IS NULL OR disabled_reason IS NOT NULL`。

## `tenant_plan`

租户套餐定义表，管理套餐编码、价格周期、额度和功能开关。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `code` | `varchar(100)` | 是 | `` | 套餐编码 |
| `name` | `varchar(200)` | 是 | `` | 套餐名称 |
| `status` | `varchar(20)` | 是 | `'ENABLED'` | 套餐状态 |
| `billing_cycle` | `varchar(20)` | 是 | `'MONTHLY'` | 计费周期 |
| `price_amount` | `numeric(18,2)` | 是 | `0` | 套餐价格 |
| `currency` | `varchar(10)` | 是 | `'CNY'` | 币种 |
| `max_user_count` | `integer` | 否 | `` | 最大用户数，空表示不限 |
| `max_warehouse_count` | `integer` | 否 | `` | 最大仓库数，空表示不限 |
| `max_storage_gb` | `numeric(18,2)` | 否 | `` | 最大存储 GB，空表示不限 |
| `feature_flags` | `jsonb` | 是 | `'{}'::jsonb` | 功能开关 JSON |
| `sort_no` | `integer` | 是 | `0` | 排序 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_tenant_plan` | `id` | primary | 主键 |
| `idx_tenant_plan_code_active` | `code; WHERE deleted_at IS NULL` | normal | 套餐编码活动态唯一；唯一性由应用逻辑保证 |
| `idx_tenant_plan_status` | `status, deleted_at` | normal | 按套餐状态筛选；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `status IN ('ENABLED', 'DISABLED')`。
- 应用层校验 `billing_cycle IN ('MONTHLY', 'YEARLY', 'PERMANENT')`。
- 应用层校验 `price_amount >= 0`。
- 应用层校验 `sort_no >= 0`。

## `tenant_plan_assignment`

租户套餐分配表，记录租户当前套餐、生效时间和变更来源。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `previous_plan_id` | `bigint` | 否 | `` | 变更前套餐 ID，应用层校验来源于 tenant_plan |
| `plan_id` | `bigint` | 是 | `` | 套餐 ID，应用层校验来源于 tenant_plan |
| `status` | `varchar(20)` | 是 | `'ACTIVE'` | 分配状态 |
| `effective_at` | `timestamptz` | 是 | `now()` | 生效时间 |
| `expires_at` | `timestamptz` | 否 | `` | 到期时间 |
| `assigned_at` | `timestamptz` | 是 | `now()` | 分配时间 |
| `assigned_by` | `varchar(100)` | 否 | `` | 分配操作人 |
| `change_reason` | `varchar(500)` | 否 | `` | 变更原因 |
| `source` | `varchar(80)` | 否 | `` | 变更来源 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_tenant_plan_assignment` | `id` | primary | 主键 |
| `idx_tenant_plan_assignment_current` | `tenant_id, status; WHERE deleted_at IS NULL` | normal | 同一租户活动套餐由应用逻辑保证唯一；唯一性由应用逻辑保证 |
| `idx_tenant_plan_assignment_plan` | `plan_id, deleted_at` | normal | 按套餐查询租户；唯一性由应用逻辑保证 |
| `idx_tenant_plan_assignment_tenant_deleted` | `tenant_id, deleted_at` | normal | 租户活动数据过滤；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `status IN ('ACTIVE', 'EXPIRED', 'CANCELLED')`。
- 应用层校验 `expires_at IS NULL OR expires_at >= effective_at`。
- 应用层校验 `previous_plan_id IS NULL OR previous_plan_id <> plan_id`。

## `tenant_domain`

租户域名表，支持租户自定义域名、独立入口和域名验证状态。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `domain` | `varchar(255)` | 是 | `` | 域名 |
| `normalized_domain` | `varchar(255)` | 是 | `` | 规范化域名，应用层统一小写并校验活动态唯一 |
| `status` | `varchar(30)` | 是 | `'PENDING'` | 域名状态 |
| `is_primary` | `boolean` | 是 | `false` | 是否主域名 |
| `verification_token` | `varchar(160)` | 否 | `` | 域名验证令牌 |
| `verified_at` | `timestamptz` | 否 | `` | 验证通过时间 |
| `last_checked_at` | `timestamptz` | 否 | `` | 最近检查时间 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_tenant_domain` | `id` | primary | 主键 |
| `idx_tenant_domain_normalized_active` | `normalized_domain; WHERE deleted_at IS NULL` | normal | 活动域名全局唯一，由应用逻辑保证；唯一性由应用逻辑保证 |
| `idx_tenant_domain_tenant` | `tenant_id, status, deleted_at` | normal | 租户域名查询；唯一性由应用逻辑保证 |
| `idx_tenant_domain_primary` | `tenant_id, is_primary, deleted_at` | normal | 主域名查询；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `status IN ('PENDING', 'VERIFIED', 'FAILED', 'DISABLED')`。
- 应用层校验 `normalized_domain = lower(domain)`。

## `tenant_contact`

租户联系人表，支持管理员、商务、技术、财务等多类型联系人。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `contact_type` | `varchar(40)` | 是 | `'ADMIN'` | 联系人类型 |
| `name` | `varchar(100)` | 是 | `` | 联系人姓名 |
| `phone` | `varchar(50)` | 否 | `` | 联系电话 |
| `email` | `varchar(200)` | 否 | `` | 联系邮箱 |
| `is_primary` | `boolean` | 是 | `false` | 是否主联系人 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_tenant_contact` | `id` | primary | 主键 |
| `idx_tenant_contact_tenant` | `tenant_id, contact_type, is_primary, deleted_at` | normal | 租户联系人查询；唯一性由应用逻辑保证 |
| `idx_tenant_contact_phone` | `phone` | normal | 联系人手机号查询；唯一性由应用逻辑保证 |
| `idx_tenant_contact_tenant_deleted` | `tenant_id, deleted_at` | normal | 租户活动数据过滤；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `contact_type IN ('ADMIN', 'BUSINESS', 'TECHNICAL', 'FINANCE', 'OTHER')`。
- 应用层校验 `phone IS NOT NULL OR email IS NOT NULL`。

## `tenant_status_history`

租户生命周期历史表，记录租户状态变化、原因、操作者和链路信息。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `from_status` | `varchar(20)` | 否 | `` | 变更前状态 |
| `to_status` | `varchar(20)` | 是 | `` | 变更后状态 |
| `change_type` | `varchar(60)` | 是 | `` | 变更类型 |
| `change_reason` | `varchar(500)` | 否 | `` | 变更原因 |
| `changed_at` | `timestamptz` | 是 | `now()` | 变更时间 |
| `changed_by` | `varchar(100)` | 否 | `` | 变更操作人 |
| `trace_id` | `varchar(100)` | 否 | `` | 链路追踪 ID |
| `request_id` | `varchar(100)` | 否 | `` | 请求 ID |
| `source` | `varchar(80)` | 否 | `` | 变更来源 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_tenant_status_history` | `id` | primary | 主键 |
| `idx_tenant_status_history_tenant` | `tenant_id, changed_at DESC, id DESC` | normal | 租户生命周期时间线查询；唯一性由应用逻辑保证 |
| `idx_tenant_status_history_trace` | `trace_id` | normal | 按 traceId 查询生命周期变更；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `from_status IS NULL OR from_status IN ('PROVISIONING', 'ENABLED', 'SUSPENDED', 'DISABLED')`。
- 应用层校验 `to_status IN ('PROVISIONING', 'ENABLED', 'SUSPENDED', 'DISABLED')`。
- 应用层校验 `from_status IS NULL OR from_status <> to_status`。

## `tenant_config`

租户级系统配置，承接旧库 app_system_config 的租户隔离配置能力。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `config_key` | `varchar(120)` | 是 | `` | 配置键 |
| `config_value` | `text` | 否 | `` | 配置值 |
| `value_type` | `varchar(40)` | 是 | `'string'` | 值类型：string/number/boolean/json |
| `description` | `varchar(500)` | 否 | `` | 配置说明 |
| `is_public` | `boolean` | 是 | `false` | 是否可被前端公开读取 |
| `is_sensitive` | `boolean` | 是 | `false` | 是否敏感配置，敏感值返回前端时必须脱敏 |
| `is_encrypted` | `boolean` | 是 | `false` | 是否已加密存储 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_tenant_config` | `id` | primary | 主键 |
| `idx_tenant_config_key_active` | `tenant_id, config_key; WHERE deleted_at IS NULL` | normal | 同一租户配置键唯一；唯一性由应用逻辑保证 |
| `idx_tenant_config_tenant_deleted` | `tenant_id, deleted_at` | normal | 租户活动数据过滤；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `value_type IN ('string', 'number', 'boolean', 'json')`。
- 应用层校验 `is_sensitive = false OR is_public = false`。
- 应用层校验 `is_encrypted = false OR is_sensitive = true`。

## `tenant_provision_task`

租户初始化任务表，记录租户开通时跨服务初始化步骤、幂等键、重试和失败原因。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `task_key` | `varchar(120)` | 是 | `` | 任务键 |
| `task_type` | `varchar(80)` | 是 | `` | 任务类型 |
| `status` | `varchar(30)` | 是 | `'PENDING'` | 任务状态 |
| `idempotency_key` | `varchar(160)` | 是 | `` | 幂等键 |
| `step_name` | `varchar(120)` | 否 | `` | 当前步骤 |
| `request_payload` | `jsonb` | 是 | `'{}'::jsonb` | 请求载荷 |
| `result_payload` | `jsonb` | 是 | `'{}'::jsonb` | 结果载荷 |
| `retry_count` | `integer` | 是 | `0` | 已重试次数 |
| `max_retry_count` | `integer` | 是 | `5` | 最大重试次数 |
| `last_error_code` | `varchar(120)` | 否 | `` | 最近错误码 |
| `last_error_message` | `text` | 否 | `` | 最近错误信息 |
| `started_at` | `timestamptz` | 否 | `` | 开始时间 |
| `finished_at` | `timestamptz` | 否 | `` | 完成时间 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_tenant_provision_task` | `id` | primary | 主键 |
| `idx_tenant_provision_task_key_active` | `tenant_id, task_key; WHERE deleted_at IS NULL` | normal | 同一租户初始化任务键唯一，由应用逻辑保证；唯一性由应用逻辑保证 |
| `idx_tenant_provision_status` | `status, updated_at` | normal | 初始化任务调度查询；唯一性由应用逻辑保证 |
| `idx_tenant_provision_idempotency` | `idempotency_key` | normal | 幂等键查询；唯一性由应用逻辑保证 |
| `idx_tenant_provision_task_tenant_deleted` | `tenant_id, deleted_at` | normal | 租户活动数据过滤；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `status IN ('PENDING', 'RUNNING', 'SUCCEEDED', 'FAILED', 'CANCELLED')`。
- 应用层校验 `retry_count >= 0`。
- 应用层校验 `max_retry_count >= 0`。

## `tenant_provision_task_step`

租户初始化任务步骤表，记录初始化任务下每个服务或业务步骤的状态、重试和结果。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `provision_task_id` | `bigint` | 是 | `` | 初始化任务 ID，应用层校验来源于 tenant_provision_task |
| `step_key` | `varchar(120)` | 是 | `` | 步骤键 |
| `step_name` | `varchar(120)` | 是 | `` | 步骤名称 |
| `status` | `varchar(30)` | 是 | `'PENDING'` | 步骤状态 |
| `sequence_no` | `integer` | 是 | `0` | 步骤顺序 |
| `idempotency_key` | `varchar(180)` | 是 | `` | 步骤幂等键 |
| `request_payload` | `jsonb` | 是 | `'{}'::jsonb` | 请求载荷 |
| `result_payload` | `jsonb` | 是 | `'{}'::jsonb` | 结果载荷 |
| `retry_count` | `integer` | 是 | `0` | 已重试次数 |
| `max_retry_count` | `integer` | 是 | `5` | 最大重试次数 |
| `last_error_code` | `varchar(120)` | 否 | `` | 最近错误码 |
| `last_error_message` | `text` | 否 | `` | 最近错误信息 |
| `started_at` | `timestamptz` | 否 | `` | 开始时间 |
| `finished_at` | `timestamptz` | 否 | `` | 完成时间 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_tenant_provision_task_step` | `id` | primary | 主键 |
| `idx_tenant_provision_step_key_active` | `provision_task_id, step_key; WHERE deleted_at IS NULL` | normal | 同一初始化任务步骤键唯一，由应用逻辑保证；唯一性由应用逻辑保证 |
| `idx_tenant_provision_step_task` | `provision_task_id, sequence_no, id` | normal | 初始化任务步骤排序查询；唯一性由应用逻辑保证 |
| `idx_tenant_provision_step_status` | `tenant_id, status, updated_at` | normal | 初始化步骤状态调度查询；唯一性由应用逻辑保证 |
| `idx_tenant_provision_step_tenant_deleted` | `tenant_id, deleted_at` | normal | 租户活动数据过滤；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `status IN ('PENDING', 'RUNNING', 'SUCCEEDED', 'FAILED', 'SKIPPED', 'CANCELLED')`。
- 应用层校验 `sequence_no >= 0`。
- 应用层校验 `retry_count >= 0`。
- 应用层校验 `max_retry_count >= 0`。

## `tenant_outbox_event`

租户服务 Outbox 事件表，在本地事务内记录待发布领域事件，避免业务写入成功但事件丢失。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `event_id` | `varchar(128)` | 是 | `` | 事件 ID |
| `aggregate_type` | `varchar(80)` | 是 | `'TENANT'` | 聚合类型 |
| `aggregate_id` | `bigint` | 是 | `` | 聚合 ID |
| `event_type` | `varchar(120)` | 是 | `` | 事件类型 |
| `topic` | `varchar(120)` | 是 | `'xuan-tenant-event'` | 事件主题 |
| `payload` | `jsonb` | 是 | `'{}'::jsonb` | 事件载荷 |
| `headers` | `jsonb` | 是 | `'{}'::jsonb` | 事件头 |
| `status` | `varchar(30)` | 是 | `'PENDING'` | 发布状态 |
| `retry_count` | `integer` | 是 | `0` | 已重试次数 |
| `max_retry_count` | `integer` | 是 | `10` | 最大重试次数 |
| `locked_by` | `varchar(100)` | 否 | `` | 当前发布锁持有者 |
| `locked_at` | `timestamptz` | 否 | `` | 锁定时间 |
| `lock_expires_at` | `timestamptz` | 否 | `` | 锁过期时间 |
| `next_retry_at` | `timestamptz` | 否 | `` | 下次重试时间 |
| `published_at` | `timestamptz` | 否 | `` | 发布时间 |
| `last_error_code` | `varchar(120)` | 否 | `` | 最近错误码 |
| `last_error_message` | `text` | 否 | `` | 最近错误信息 |
| `first_failed_at` | `timestamptz` | 否 | `` | 首次失败时间 |
| `dead_letter_at` | `timestamptz` | 否 | `` | 进入死信时间 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_tenant_outbox_event` | `id` | primary | 主键 |
| `idx_tenant_outbox_status` | `status, next_retry_at, id` | normal | 待发布事件调度查询；唯一性由应用逻辑保证 |
| `idx_tenant_outbox_lock` | `status, lock_expires_at, id` | normal | 发布锁超时扫描；唯一性由应用逻辑保证 |
| `idx_tenant_outbox_dead_letter` | `status, dead_letter_at, id` | normal | 死信事件扫描；唯一性由应用逻辑保证 |
| `idx_tenant_outbox_event` | `event_id` | normal | 事件 ID 查询；唯一性由应用逻辑保证 |
| `idx_tenant_outbox_aggregate` | `aggregate_type, aggregate_id, id` | normal | 聚合事件查询；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `status IN ('PENDING', 'PUBLISHING', 'PUBLISHED', 'FAILED', 'DEAD_LETTERED')`。
- 应用层校验 `retry_count >= 0`。
- 应用层校验 `max_retry_count >= 0`。
- 应用层校验 `locked_at IS NULL OR lock_expires_at IS NOT NULL`。
- 应用层校验 `dead_letter_at IS NULL OR status = 'DEAD_LETTERED'`。
