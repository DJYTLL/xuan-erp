---
title: "xuan-product 数据库结构"
---

本文记录 `xuan-product` 的数据库结构、表字段、普通索引、初始化数据和 Flyway migration 对应关系。

## 数据库归属

| 项 | 内容 |
| --- | --- |
| 数据库 | `xuan_product` |
| migration 目录 | `xuan-product/src/main/resources/db/migration/` |
| 当前 migration 最新版本 | `V1` |
| 本次新增 migration | `V1__init_product_database.sql` |
| 数据所有权 | 只允许 `xuan-product` 直接写入商品、分类、单位、价格和车型适配数据 |

## migration 规则

涉及数据库结构、字段、索引、约束、初始化数据或 Flyway 脚本时，必须先扫描本服务 `db/migration` 目录，确认当前最高版本号，再追加新 migration。禁止跳号、复用版本号、修改历史 migration。

本次扫描结果：本服务历史最高版本为“无”，新增 `V1`，不存在版本冲突。

## 设计说明

- 本服务使用独立数据库 `xuan_product`，服务启动时 Flyway 连接该数据库执行 migration。
- migration 只管理当前连接数据库内的表、索引、约束和初始化数据；不包含 `CREATE DATABASE`，也不创建 PostgreSQL schema。
- 跨服务只保存外部 ID、编码和必要快照字段，不建立跨服务数据库外键。
- 数据库只保留表结构、主键和普通查询索引；外键、唯一性、状态枚举、金额非负等业务约束全部由应用逻辑校验。
- 业务表默认包含 `tenant_id`，平台级或网关级表会在表说明中单独说明。
- 逻辑删除统一使用 `deleted_at`，活动态唯一性由应用层在 `deleted_at IS NULL` 范围内校验。

## 核心表清单

| 表名 | 说明 | 是否包含 tenant_id |
| --- | --- | --- |
| `product_category` | 商品分类表。 | 是 |
| `product_unit` | 计量单位表。 | 是 |
| `vehicle_brand` | 车型品牌表。 | 是 |
| `vehicle_series` | 车型车系表。 | 是 |
| `vehicle_model` | 车型表。 | 是 |
| `product` | 商品主档表。 | 是 |
| `product_price` | 商品按客户类别的售价表。 | 是 |
| `product_fitment` | 商品适配车型关系表。 | 是 |
| `product_import_batch` | 商品导入批次。 | 是 |
| `product_import_item` | 商品导入明细。 | 是 |
| `undo_log` | Seata AT 模式回滚日志表 | 否 |

## 初始化数据

| 表名 | 初始化条目 | 说明 |
| --- | --- | --- |
| `product_category` | `DEFAULT` | 默认商品分类，便于首批商品档案落位。 |
| `product_unit` | `PCS`, `SET`, `BOX`, `BOTTLE`, `LITER` | 常用计量单位，后续可按租户继续维护。 |

## `product_category`

商品分类表。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `code` | `varchar(100)` | 是 | `` | 分类编码 |
| `name` | `varchar(200)` | 是 | `` | 分类名称 |
| `parent_id` | `bigint` | 否 | `` | 父级分类 ID |
| `level_no` | `integer` | 是 | `1` | 层级 |
| `sort_no` | `integer` | 是 | `0` | 排序 |
| `is_enabled` | `boolean` | 是 | `true` | 是否启用 |
| `is_default` | `boolean` | 是 | `false` | 是否默认分类 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_product_category` | `id` | primary | 主键 |
| `idx_product_category_code_active` | `tenant_id, code; WHERE deleted_at IS NULL` | normal | 分类编码活动态唯一；唯一性由应用逻辑保证 |
| `idx_product_category_default` | `tenant_id; WHERE is_default = true AND deleted_at IS NULL` | normal | 每租户一个默认分类；唯一性由应用逻辑保证 |
| `idx_product_category_parent` | `tenant_id, parent_id` | normal | 分类树查询；唯一性由应用逻辑保证 |
| `idx_product_category_tenant_deleted` | `tenant_id, deleted_at` | normal | 租户活动数据过滤；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `parent_id` 必须引用有效的 `product_category.id`。
- 应用层校验 `level_no >= 0`。
- 应用层校验 `sort_no >= 0`。

## `product_unit`

计量单位表。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `code` | `varchar(100)` | 是 | `` | 单位编码 |
| `name` | `varchar(200)` | 是 | `` | 单位名称 |
| `symbol` | `varchar(50)` | 否 | `` | 单位符号 |
| `decimal_precision` | `integer` | 是 | `0` | 数量小数位 |
| `is_enabled` | `boolean` | 是 | `true` | 是否启用 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_product_unit` | `id` | primary | 主键 |
| `idx_product_unit_code_active` | `tenant_id, code; WHERE deleted_at IS NULL` | normal | 单位编码活动态唯一；唯一性由应用逻辑保证 |
| `idx_product_unit_tenant_deleted` | `tenant_id, deleted_at` | normal | 租户活动数据过滤；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `decimal_precision >= 0`。

## `vehicle_brand`

车型品牌表。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `code` | `varchar(50)` | 是 | `` | 品牌编码 |
| `name` | `varchar(100)` | 是 | `` | 品牌名称 |
| `is_enabled` | `boolean` | 是 | `true` | 是否启用 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_vehicle_brand` | `id` | primary | 主键 |
| `idx_product_vehicle_brand_code_active` | `tenant_id, code; WHERE deleted_at IS NULL` | normal | 品牌编码活动态唯一；唯一性由应用逻辑保证 |
| `idx_product_vehicle_brand_name` | `tenant_id, name` | normal | 品牌名称查询；唯一性由应用逻辑保证 |
| `idx_product_vehicle_brand_tenant_deleted` | `tenant_id, deleted_at` | normal | 租户活动数据过滤；唯一性由应用逻辑保证 |

## `vehicle_series`

车型车系表。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `brand_id` | `bigint` | 是 | `` | 品牌 ID |
| `code` | `varchar(50)` | 是 | `` | 车系编码 |
| `name` | `varchar(100)` | 是 | `` | 车系名称 |
| `is_enabled` | `boolean` | 是 | `true` | 是否启用 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_vehicle_series` | `id` | primary | 主键 |
| `idx_product_vehicle_series_code_active` | `tenant_id, brand_id, code; WHERE deleted_at IS NULL` | normal | 同品牌下车系编码活动态唯一；唯一性由应用逻辑保证 |
| `idx_product_vehicle_series_brand` | `tenant_id, brand_id` | normal | 品牌下车系查询；唯一性由应用逻辑保证 |
| `idx_product_vehicle_series_tenant_deleted` | `tenant_id, deleted_at` | normal | 租户活动数据过滤；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `brand_id` 必须引用有效的 `vehicle_brand.id`。

## `vehicle_model`

车型表。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `series_id` | `bigint` | 是 | `` | 车系 ID |
| `code` | `varchar(50)` | 是 | `` | 车型编码 |
| `name` | `varchar(120)` | 是 | `` | 车型名称 |
| `year_from` | `integer` | 否 | `` | 起始年款 |
| `year_to` | `integer` | 否 | `` | 结束年款 |
| `displacement` | `varchar(50)` | 否 | `` | 排量 |
| `engine` | `varchar(100)` | 否 | `` | 发动机型号 |
| `is_enabled` | `boolean` | 是 | `true` | 是否启用 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_vehicle_model` | `id` | primary | 主键 |
| `idx_product_vehicle_model_code_active` | `tenant_id, series_id, code; WHERE deleted_at IS NULL` | normal | 同车系下车型编码活动态唯一；唯一性由应用逻辑保证 |
| `idx_product_vehicle_model_series` | `tenant_id, series_id` | normal | 车系下车型查询；唯一性由应用逻辑保证 |
| `idx_product_vehicle_model_tenant_deleted` | `tenant_id, deleted_at` | normal | 租户活动数据过滤；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `series_id` 必须引用有效的 `vehicle_series.id`。

## `product`

商品主档表。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `code` | `varchar(100)` | 是 | `` | 商品编码 |
| `name` | `varchar(200)` | 是 | `` | 商品名称 |
| `short_name` | `varchar(100)` | 否 | `` | 商品简称 |
| `spec` | `varchar(200)` | 否 | `` | 规格 |
| `model` | `varchar(200)` | 否 | `` | 型号 |
| `category_id` | `bigint` | 否 | `` | 分类 ID |
| `unit_id` | `bigint` | 否 | `` | 单位 ID |
| `barcode` | `varchar(100)` | 否 | `` | 条码 |
| `sku` | `varchar(100)` | 否 | `` | SKU |
| `brand` | `varchar(100)` | 否 | `` | 品牌 |
| `origin` | `varchar(200)` | 否 | `` | 产地 |
| `weight` | `numeric(18,4)` | 否 | `` | 重量 |
| `volume` | `numeric(18,4)` | 否 | `` | 体积 |
| `cost_price` | `numeric(18,4)` | 是 | `0` | 成本价 |
| `sale_price` | `numeric(18,4)` | 是 | `0` | 销售价 |
| `tax_rate` | `numeric(10,4)` | 是 | `0` | 默认税率 |
| `safety_stock` | `numeric(18,4)` | 是 | `0` | 默认安全库存 |
| `min_stock` | `numeric(18,4)` | 是 | `0` | 默认最低库存 |
| `max_stock` | `numeric(18,4)` | 是 | `0` | 默认最高库存 |
| `is_batch` | `boolean` | 是 | `false` | 是否批次管理 |
| `shelf_life_days` | `integer` | 否 | `` | 保质期天数 |
| `product_type` | `varchar(20)` | 是 | `'NORMAL'` | 商品类型 |
| `manufacturer_code` | `varchar(100)` | 否 | `` | 厂家编码 |
| `manufacturer_model` | `varchar(200)` | 否 | `` | 厂家型号 |
| `manufacturer_name` | `varchar(200)` | 否 | `` | 厂家名称 |
| `default_warehouse_id` | `bigint` | 否 | `` | 默认仓库 ID |
| `default_warehouse_name_snapshot` | `varchar(200)` | 否 | `` | 默认仓库名称快照 |
| `default_location_id` | `bigint` | 否 | `` | 默认库位 ID |
| `default_location_name_snapshot` | `varchar(200)` | 否 | `` | 默认库位名称快照 |
| `source_supplier_id` | `bigint` | 否 | `` | 来源供应商 ID |
| `source_supplier_name_snapshot` | `varchar(200)` | 否 | `` | 来源供应商名称快照 |
| `is_enabled` | `boolean` | 是 | `true` | 是否启用 |
| `ext_attrs` | `jsonb` | 否 | `` | 扩展属性 |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_product` | `id` | primary | 主键 |
| `idx_product_product_code_active` | `tenant_id, code; WHERE deleted_at IS NULL` | normal | 商品编码活动态唯一；唯一性由应用逻辑保证 |
| `idx_product_name` | `tenant_id, name` | normal | 商品名称查询；唯一性由应用逻辑保证 |
| `idx_product_manufacturer_code` | `tenant_id, manufacturer_code; WHERE deleted_at IS NULL` | normal | 厂家编码查询；唯一性由应用逻辑保证 |
| `idx_product_product_tenant_deleted` | `tenant_id, deleted_at` | normal | 租户活动数据过滤；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `category_id` 必须引用有效的 `product_category.id`。
- 应用层校验 `unit_id` 必须引用有效的 `product_unit.id`。
- 应用层校验 `product_type IN ('NORMAL', 'ASSEMBLY')`。
- 应用层校验 `weight IS NULL OR weight >= 0`。
- 应用层校验 `volume IS NULL OR volume >= 0`。
- 应用层校验 `shelf_life_days IS NULL OR shelf_life_days >= 0`。
- 应用层校验 `cost_price >= 0`。
- 应用层校验 `sale_price >= 0`。
- 应用层校验 `tax_rate >= 0`。
- 应用层校验 `safety_stock >= 0`。
- 应用层校验 `min_stock >= 0`。
- 应用层校验 `max_stock >= 0`。

### 跨服务引用

- `default_warehouse_id`、`default_location_id` 引用 `xuan-warehouse`，只保存 ID 和名称快照。
- `source_supplier_id` 引用 `xuan-party.supplier`，只保存 ID 和名称快照。

## `product_price`

商品按客户类别的售价表。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `product_id` | `bigint` | 是 | `` | 商品 ID |
| `customer_category_id` | `bigint` | 是 | `` | 客户类别 ID |
| `customer_category_name_snapshot` | `varchar(200)` | 否 | `` | 客户类别名称快照 |
| `sale_price` | `numeric(18,4)` | 是 | `0` | 售价 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_product_price` | `id` | primary | 主键 |
| `idx_product_product_price_key_active` | `tenant_id, product_id, customer_category_id; WHERE deleted_at IS NULL` | normal | 商品客户类别售价活动态唯一；唯一性由应用逻辑保证 |
| `idx_product_price_product` | `tenant_id, product_id, deleted_at` | normal | 商品价格查询；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `product_id` 必须引用有效的 `product.id`。
- 应用层校验 `sale_price >= 0`。

### 跨服务引用

- `customer_category_id` 引用 `xuan-party.customer_category`，不建数据库外键。

## `product_fitment`

商品适配车型关系表。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `product_id` | `bigint` | 是 | `` | 商品 ID |
| `model_id` | `bigint` | 是 | `` | 车型 ID |
| `remark` | `varchar(500)` | 否 | `` | 备注 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_product_fitment` | `id` | primary | 主键 |
| `idx_product_fitment_key_active` | `tenant_id, product_id, model_id; WHERE deleted_at IS NULL` | normal | 商品车型适配活动态唯一；唯一性由应用逻辑保证 |
| `idx_product_fitment_model` | `tenant_id, model_id` | normal | 按车型查商品；唯一性由应用逻辑保证 |
| `idx_product_fitment_tenant_deleted` | `tenant_id, deleted_at` | normal | 租户活动数据过滤；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `product_id` 必须引用有效的 `product.id`。
- 应用层校验 `model_id` 必须引用有效的 `vehicle_model.id`。

## `product_import_batch`

商品导入批次。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `batch_no` | `varchar(64)` | 是 | `` | 导入批次号 |
| `source_name` | `varchar(255)` | 否 | `` | 来源文件名 |
| `import_mode` | `varchar(32)` | 是 | `'EXCEL_UPLOAD'` | 导入模式 |
| `total_count` | `integer` | 是 | `0` | 总数 |
| `success_count` | `integer` | 是 | `0` | 成功数 |
| `failed_count` | `integer` | 是 | `0` | 失败数 |
| `warning_count` | `integer` | 是 | `0` | 警告数 |
| `status` | `varchar(32)` | 是 | `'PENDING'` | 状态 |
| `summary` | `text` | 否 | `` | 导入摘要 |
| `raw_payload` | `jsonb` | 否 | `` | 原始载荷 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_product_import_batch` | `id` | primary | 主键 |
| `idx_product_impo_batch_no_active` | `tenant_id, batch_no; WHERE deleted_at IS NULL` | normal | 批次号活动态唯一；唯一性由应用逻辑保证 |
| `idx_product_import_batch_status` | `tenant_id, status; WHERE deleted_at IS NULL` | normal | 导入状态查询；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `status IN ('PENDING','PROCESSING','SUCCESS','FAILED','PARTIAL_SUCCESS')`。
- 应用层校验 `total_count >= 0`。
- 应用层校验 `success_count >= 0`。
- 应用层校验 `failed_count >= 0`。
- 应用层校验 `warning_count >= 0`。
- 应用层校验 `success_count + failed_count <= total_count`。

## `product_import_item`

商品导入明细。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint GENERATED BY DEFAULT AS IDENTITY` | 是 | `` | 主键 |
| `tenant_id` | `bigint` | 是 | `` | 租户 ID，来源于 xuan-tenant |
| `batch_id` | `bigint` | 是 | `` | 导入批次 ID |
| `row_no` | `integer` | 是 | `` | 行号 |
| `source_code` | `varchar(128)` | 否 | `` | 来源编码 |
| `source_name` | `varchar(255)` | 否 | `` | 来源名称 |
| `matched_target_id` | `bigint` | 否 | `` | 匹配目标 ID |
| `status` | `varchar(32)` | 是 | `'PENDING'` | 状态 |
| `error_field` | `varchar(128)` | 否 | `` | 错误字段 |
| `error_message` | `text` | 否 | `` | 错误信息 |
| `warning_message` | `text` | 否 | `` | 警告信息 |
| `raw_row` | `jsonb` | 是 | `` | 原始行 |
| `normalized_payload` | `jsonb` | 否 | `` | 规范化数据 |
| `created_by` | `varchar(100)` | 否 | `` | 创建人 |
| `created_at` | `timestamptz` | 是 | `now()` | 创建时间 |
| `updated_by` | `varchar(100)` | 否 | `` | 更新人 |
| `updated_at` | `timestamptz` | 是 | `now()` | 更新时间 |
| `deleted_by` | `varchar(100)` | 否 | `` | 删除人 |
| `delete_reason` | `varchar(500)` | 否 | `` | 删除原因 |
| `deleted_at` | `timestamptz` | 否 | `` | 逻辑删除时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_product_import_item` | `id` | primary | 主键 |
| `idx_product_impo_batch_row_active` | `tenant_id, batch_id, row_no; WHERE deleted_at IS NULL` | normal | 同批次行号活动态唯一；唯一性由应用逻辑保证 |
| `idx_product_import_item_status` | `tenant_id, status; WHERE deleted_at IS NULL` | normal | 导入明细状态查询；唯一性由应用逻辑保证 |

### 逻辑约束

- 应用层校验 `batch_id` 必须引用有效的 `product_import_batch.id`。
- 应用层校验 `status IN ('PENDING','PROCESSING','SUCCESS','FAILED','PARTIAL_SUCCESS')`。
- 应用层校验 `row_no >= 1`。

## `undo_log`

Seata AT 模式回滚日志表，用于全局事务回滚。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `branch_id` | `bigint` | 是 | `` | Seata 分支事务 ID |
| `xid` | `varchar(128)` | 是 | `` | Seata 全局事务 ID |
| `context` | `varchar(128)` | 是 | `` | 上下文 |
| `rollback_info` | `bytea` | 是 | `` | 回滚信息 |
| `log_status` | `integer` | 是 | `` | 日志状态 |
| `log_created` | `timestamp(6)` | 是 | `` | 创建时间 |
| `log_modified` | `timestamp(6)` | 是 | `` | 更新时间 |

### 主键与普通索引

| 名称 | 字段/表达式 | 类型 | 说明 |
| --- | --- | --- | --- |
| `pk_undo_log` | `branch_id, xid` | primary | 主键 |
| `idx_undo_log_log_created` | `log_created` | normal | 按创建时间清理和排查回滚日志 |
