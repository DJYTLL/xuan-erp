service: xuan-tenant
draft: true
columns:
  - resourceKey: tenant
    menuCode: tenant-management
    browseFields:
      - code
      - name
      - status
      - currentPlanName
      - currentPlanExpiresAt
      - primaryDomain
      - contactName
      - contactPhone
      - provisionedAt
      - permissionSyncStatus
    detailFields:
      - remark
    note: 与 tenant-management 浏览表 schema、详情字段配置和 IAM tenant 列资源保持一致
