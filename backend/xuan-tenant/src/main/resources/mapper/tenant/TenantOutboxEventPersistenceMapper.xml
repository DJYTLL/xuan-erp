<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuan.erp.tenant.infrastructure.persistence.mapper.TenantOutboxEventPersistenceMapper">

    <resultMap id="TenantOutboxEventRecordMap" type="com.xuan.erp.tenant.infrastructure.persistence.entity.TenantOutboxEventRecord">
        <constructor>
            <idArg column="id" javaType="java.lang.Long"/>
            <arg column="tenant_id" javaType="java.lang.Long"/>
            <arg column="event_id" javaType="java.lang.String"/>
            <arg column="aggregate_type" javaType="java.lang.String"/>
            <arg column="aggregate_id" javaType="java.lang.Long"/>
            <arg column="event_type" javaType="java.lang.String"/>
            <arg column="topic" javaType="java.lang.String"/>
            <arg column="payload" javaType="java.lang.String"/>
            <arg column="headers" javaType="java.lang.String"/>
            <arg column="status" javaType="java.lang.String"/>
            <arg column="retry_count" javaType="_int"/>
            <arg column="max_retry_count" javaType="_int"/>
            <arg column="locked_by" javaType="java.lang.String"/>
            <arg column="locked_at" javaType="java.time.OffsetDateTime"/>
            <arg column="lock_expires_at" javaType="java.time.OffsetDateTime"/>
            <arg column="next_retry_at" javaType="java.time.OffsetDateTime"/>
            <arg column="published_at" javaType="java.time.OffsetDateTime"/>
            <arg column="last_error_code" javaType="java.lang.String"/>
            <arg column="last_error_message" javaType="java.lang.String"/>
            <arg column="first_failed_at" javaType="java.time.OffsetDateTime"/>
            <arg column="dead_letter_at" javaType="java.time.OffsetDateTime"/>
            <arg column="created_by" javaType="java.lang.String"/>
            <arg column="created_at" javaType="java.time.OffsetDateTime"/>
            <arg column="updated_by" javaType="java.lang.String"/>
            <arg column="updated_at" javaType="java.time.OffsetDateTime"/>
        </constructor>
    </resultMap>

    <select id="findById" resultMap="TenantOutboxEventRecordMap">
        SELECT * FROM tenant_outbox_event WHERE id = #{id}
    </select>

    <select id="findLatestByEventId" resultMap="TenantOutboxEventRecordMap">
        SELECT * FROM tenant_outbox_event
        WHERE event_id = #{eventId}
        ORDER BY id DESC
        LIMIT 1
    </select>

    <select id="findPublishable" resultMap="TenantOutboxEventRecordMap">
        SELECT * FROM tenant_outbox_event
        WHERE status IN ('PENDING', 'FAILED')
          AND retry_count &lt; max_retry_count
          AND dead_letter_at IS NULL
          AND (next_retry_at IS NULL OR next_retry_at &lt;= #{now})
        ORDER BY id ASC
        LIMIT #{limit}
    </select>

    <insert id="insert">
        INSERT INTO tenant_outbox_event (
            tenant_id, event_id, aggregate_type, aggregate_id, event_type, topic,
            payload, headers, status, retry_count, max_retry_count,
            locked_by, locked_at, lock_expires_at, next_retry_at, published_at,
            last_error_code, last_error_message, first_failed_at, dead_letter_at,
            created_by, created_at, updated_by, updated_at
        ) VALUES (
            #{tenantId}, #{eventId}, #{aggregateType}, #{aggregateId}, #{eventType}, #{topic},
            CAST(#{payloadJson} AS jsonb), CAST(#{headersJson} AS jsonb), #{status}, #{retryCount}, #{maxRetryCount},
            #{lockedBy}, #{lockedAt}, #{lockExpiresAt}, #{nextRetryAt}, #{publishedAt},
            #{lastErrorCode}, #{lastErrorMessage}, #{firstFailedAt}, #{deadLetterAt},
            #{createdBy}, #{createdAt}, #{updatedBy}, #{updatedAt}
        )
    </insert>

    <update id="update">
        UPDATE tenant_outbox_event
        SET tenant_id = #{tenantId},
            event_id = #{eventId},
            aggregate_type = #{aggregateType},
            aggregate_id = #{aggregateId},
            event_type = #{eventType},
            topic = #{topic},
            payload = CAST(#{payloadJson} AS jsonb),
            headers = CAST(#{headersJson} AS jsonb),
            status = #{status},
            retry_count = #{retryCount},
            max_retry_count = #{maxRetryCount},
            locked_by = #{lockedBy},
            locked_at = #{lockedAt},
            lock_expires_at = #{lockExpiresAt},
            next_retry_at = #{nextRetryAt},
            published_at = #{publishedAt},
            last_error_code = #{lastErrorCode},
            last_error_message = #{lastErrorMessage},
            first_failed_at = #{firstFailedAt},
            dead_letter_at = #{deadLetterAt},
            created_by = #{createdBy},
            created_at = #{createdAt},
            updated_by = #{updatedBy},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>
</mapper>
