<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuan.erp.tenant.infrastructure.persistence.mapper.TenantConfigPersistenceMapper">

    <resultMap id="TenantConfigRecordMap" type="com.xuan.erp.tenant.infrastructure.persistence.entity.TenantConfigRecord">
        <constructor>
            <idArg column="id" javaType="java.lang.Long"/>
            <arg column="tenant_id" javaType="java.lang.Long"/>
            <arg column="config_key" javaType="java.lang.String"/>
            <arg column="config_value" javaType="java.lang.String"/>
            <arg column="value_type" javaType="java.lang.String"/>
            <arg column="description" javaType="java.lang.String"/>
            <arg column="is_public" javaType="_boolean"/>
            <arg column="is_sensitive" javaType="_boolean"/>
            <arg column="is_encrypted" javaType="_boolean"/>
            <arg column="created_by" javaType="java.lang.String"/>
            <arg column="created_at" javaType="java.time.OffsetDateTime"/>
            <arg column="updated_by" javaType="java.lang.String"/>
            <arg column="updated_at" javaType="java.time.OffsetDateTime"/>
            <arg column="deleted_by" javaType="java.lang.String"/>
            <arg column="delete_reason" javaType="java.lang.String"/>
            <arg column="deleted_at" javaType="java.time.OffsetDateTime"/>
        </constructor>
    </resultMap>

    <select id="findById" resultMap="TenantConfigRecordMap">
        SELECT * FROM tenant_config WHERE id = #{id} AND deleted_at IS NULL
    </select>

    <select id="findByIdIncludingDeleted" resultMap="TenantConfigRecordMap">
        SELECT * FROM tenant_config WHERE id = #{id}
    </select>

    <select id="findActiveByTenantIdAndKey" resultMap="TenantConfigRecordMap">
        SELECT * FROM tenant_config
        WHERE tenant_id = #{tenantId}
          AND config_key = #{configKey}
          AND deleted_at IS NULL
        ORDER BY id
        LIMIT 1
    </select>

    <select id="findByTenantId" resultMap="TenantConfigRecordMap">
        SELECT * FROM tenant_config
        WHERE tenant_id = #{tenantId}
          AND deleted_at IS NULL
        ORDER BY id
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByTenantId" resultType="long">
        SELECT COUNT(*) FROM tenant_config WHERE tenant_id = #{tenantId} AND deleted_at IS NULL
    </select>

    <select id="findPublicByTenantId" resultMap="TenantConfigRecordMap">
        SELECT * FROM tenant_config
        WHERE tenant_id = #{tenantId}
          AND is_public = true
          AND is_sensitive = false
          AND deleted_at IS NULL
        ORDER BY id
    </select>

    <insert id="insert">
        INSERT INTO tenant_config (
            tenant_id, config_key, config_value, value_type, description,
            is_public, is_sensitive, is_encrypted,
            created_by, created_at, updated_by, updated_at, deleted_by, delete_reason, deleted_at
        ) VALUES (
            #{tenantId}, #{configKey}, #{configValue}, #{valueType}, #{description},
            #{publicConfig}, #{sensitive}, #{encrypted},
            #{createdBy}, #{createdAt}, #{updatedBy}, #{updatedAt}, #{deletedBy}, #{deleteReason}, #{deletedAt}
        )
    </insert>

    <update id="update">
        UPDATE tenant_config
        SET tenant_id = #{tenantId},
            config_key = #{configKey},
            config_value = #{configValue},
            value_type = #{valueType},
            description = #{description},
            is_public = #{publicConfig},
            is_sensitive = #{sensitive},
            is_encrypted = #{encrypted},
            created_by = #{createdBy},
            created_at = #{createdAt},
            updated_by = #{updatedBy},
            updated_at = #{updatedAt},
            deleted_by = #{deletedBy},
            delete_reason = #{deleteReason},
            deleted_at = #{deletedAt}
        WHERE id = #{id}
    </update>
</mapper>
