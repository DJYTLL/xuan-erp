<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuan.erp.iam.infrastructure.persistence.mapper.IamTenantBootstrapMapper">

    <select id="bootstrapTenant" resultType="java.lang.Integer">
        SELECT bootstrap_iam_tenant(
            #{tenantId},
            #{adminUsername},
            #{adminPasswordHash},
            #{adminDisplayName},
            #{adminEmail},
            #{adminPhone},
            #{iamInitTemplateCode},
            #{requestedBy}
        )
    </select>
</mapper>
