spring:
  config:
    import:
      - optional:nacos:xuan-infra.yaml
      - optional:nacos:xuan-common.yaml
      - optional:nacos:xuan-finance.yaml
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: duaoyunxuan.com:9041
        namespace: dev
        group: XUAN_ERP_GROUP
      config:
        server-addr: duaoyunxuan.com:9041
        namespace: dev
        group: XUAN_ERP_GROUP
        file-extension: yaml
