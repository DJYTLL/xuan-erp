# 前端浏览器请求统一走 gateway，开发环境默认代理到 http://127.0.0.1:8100
# 不再支持通过 VITE_API_BASE_URL 或 VITE_DEV_PROXY_TARGET 直连业务服务
# 本地推荐固定访问 http://127.0.0.1:5173；localhost:5173 会在开发服务中重定向到该入口
# 浏览器 localStorage 按 origin 隔离，127.0.0.1 和 localhost 不共享登录态，所以开发态统一使用 127.0.0.1
